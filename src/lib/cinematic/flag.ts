// Build-time constant (inlined by Next) — flag-off builds tree-shake the
// entire cinematic layer and every CTA keeps its conventional /contact link.
// Resolution rules live in next.config.ts.
export const CINEMATIC_ENABLED = process.env.NEXT_PUBLIC_CINEMATIC_EXPERIENCE === "true";
